Nest API for the Ziptrrip todo assignment.

From this folder:

```
cp .env.example .env
npm install
npm run start:dev
```

Mongo must already be running. See the root README for Docker, tests, and the HTTP contract.
